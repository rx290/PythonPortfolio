"""
Write a program to intake strings into an array/list make all even numbered strings uppercase.
Concatenate the strings in a single one in the end.
"""
arr = []

for i in range(5):
    arr.append(input("Input {} String: ".format(i+1)))
