# Web Chat App using Flask SocketIO

## Introduction

    SocketIO is a library which keeps an active connection with the server so that we can directly communicate with it without waiting for request or response.

    